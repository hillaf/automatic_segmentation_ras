# Plotting Code

Plotting belongs in this folder and should import computed values from
`submission_code.publication_tables` or the lower-level evaluation modules.
Evaluation code must not import from this folder.

The journal submission tables are generated by:

```bash
.venv/bin/python -m submission_code.publication_tables
```

